import React, {useState, useEffect} from 'react';

function Loading() {

    return (
        <div className="h-screen w-screen m-auto items-center">
            Loading...
        </div>
    );
}

export default Loading;